#include <string>
#include <iostream>

template <typename T> 
class Aray
{
	private :
	public:
		Array(void);
		Array(unsigned int n);
		Array(const other & Array)
		Array& operator=(const Array& other);
		~Array();
}